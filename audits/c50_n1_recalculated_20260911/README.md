# Recalculated C50/N1 review list, 2026-09-11

**User accepted the corrected inputs on 2026-09-11:** "Now C50/N1 Looks ok
to me." All 18 reviewed mode/continuum fingerprints were rechecked before
removing the C50/N1 exclusion. The other five registry entries remain active.
This is input-validity acceptance; ordinary morphology gates still apply.

[current_n1_modes.csv](current_n1_modes.csv) contains all **18 current N1
mode files** in DiTw `nstxuG142301C50`, ordered by binary-header frequency.
It has no GOOD/BAD labels and includes all frequencies, without gap routing.
All files load successfully with ntor=1, nr=201 and 25 stored harmonics;
signed structures and frequencies are finite, and the shared continuum
loader succeeds. This checks readability, not continuum/resonance alignment.

The last installed rules CSV captured eight of these files during the
recalculation on September 10. Their mode-plus-continuum fingerprints are
unchanged; ten filenames have appeared since that snapshot. None of the
18 filenames appears in the original September 9 list of 73 invalidated
modes. All 73 original filenames are absent from the current N1 directory.

Open the full current set from the repository using the configured NOVA
environment (`NOVA_DITW_ROOT` points to DiTw):

```tcsh
python viz/view_modes_csv.py audits/c50_n1_recalculated_20260911/current_n1_modes.csv --base_dir "$NOVA_DITW_ROOT"
```

The viewer reader was checked against all 18 paths and assigns no labels.
The mode inventory and source hashes were stable across the check. Individual
mode hashes and paired input fingerprints are in the CSV; baseline, continuum,
registry and CSV hashes are in [receipt.json](receipt.json).

The initial list fulfilled the requested raw-list alternative before user
review. Its `receipt.json` is a historical snapshot: no sorter had yet been
rerun, and the C50/N1 exclusion was still active at that point. The subsequent
user acceptance above supersedes that pending-review status.

## Rules results after acceptance

Regenerated the whole shot with production rules v11 and separately ran
`sort_shot_rules.py` on N1 with the same named configuration. N1 routing and
gate results agree exactly between the two runs:

- 10 N1 modes route to EAE-like, outside the TAE morphology classifier.
- 8 N1 modes are TAE-like. Seven fail `BAD_AXIS_SPIKE`; N1/5347 fails
  `BAD_CONT_CROSS_WINDOW`. There are no N1 TAE-side survivors.
- All 18 are valid inputs; none is excluded as `KNOWN_INVALID_INPUT`.

[n1_classifications.csv](n1_classifications.csv) contains the per-mode results.
The full shot has 556 inputs, 202 TAE-side modes (193 TAE-like / 9 mixed),
354 EAE-like, zero INVALID and zero final GOOD. All 538 N2–N10 audit rows
are exactly unchanged from the preceding production output. All 202 TAE-side
severity reports are complete; there are no resolution warnings. All input
fingerprints and the inventory stayed unchanged during sorting.

The verified production output is installed at
`$NOVA_SORT_OUTPUTS/nstxuG142301C50`, where `NOVA_SORT_OUTPUTS` denotes the
existing rules output root. Its preceding directory is preserved at
`$NOVA_SORT_OUTPUTS/before_c50_n1_restored_20260911/nstxuG142301C50`.
The exact paths and tree hashes are in
[restoration_publication.json](restoration_publication.json).
[restoration_verification.json](restoration_verification.json) records the
review, configuration/registry hashes, counts and checks. Four existing
input-validity tests pass after updating their per-N fixture to B12 and
checking explicitly that C50/N1 is cleared.

Reproduce from the repository in the configured NOVA environment:

```tcsh
python scripts/sort_shot_mixed.py --method rules --shot_dir "$NOVA_DITW_ROOT/nstxuG142301C50" --out_dir outputs/review_c50_n1_rerun/rules
python scripts/sort_shot_rules.py --rule_config tae_rules_production_v11 --n_min 1 --n_max 1 --shot_dir "$NOVA_DITW_ROOT/nstxuG142301C50" --out_dir outputs/review_c50_n1_rerun/calibration_n1
```

Full exports, logs and run-input provenance remain ignored under
`outputs/review_c50_n1_restored_20260911/`. Existing RF-CNN exports and
disagreement CSVs are historical and were not regenerated by this rules-only
refresh; their old C50/N1 INVALID entries do not describe the corrected inputs.
Active training labels and all other shot exclusions are unchanged.
