# E204678M01t017 N9/1081: why the rules retain it

The user queried the narrow structure and appreciable continuum-crossing
amplitude in `nstxuE204678M01t017/N9/egn09w.1081E+02`. This is a non-blind
diagnostic follow-up to row 24 of the pilot disagreement CSV.

The live mode/continuum fingerprint remains
`2ed976a57c0f373ca7e1b2d15b1abe9d74ee6f22f57c2bc192275b61af0d81a4`.
A fresh single-mode calibration run with production-v11 configuration
reproduces the saved `rule_features` object exactly and returns REVIEW
(`NO_GOOD_TEMPLATE`). Production's survivor policy promotes that result to
GOOD. Native nr=201, stored harmonic count=94; no missing diagnostic or
resolution fallback explains the retention.

| Diagnostic | Measured | Active rejection condition / scope |
| --- | --- | --- |
| Strongest signed lobe | amplitude 1, r=0.555, full half-maximum width 1.16708 grid intervals | amplitude >=0.3 and width <=1 interval at this radius |
| Total radial energy envelope | peak r=0.555, FWHM 1.05287 intervals | width <=2, but only for peak r<=0.5, subject to extremum exception |
| Upper-continuum crossing | r=0.502667565 | true crossing found |
| Maximum amplitude in crossing +/-0.01 window | 0.204943 at r=0.505 | >=0.25 |
| Maximum normalized radial energy in that window | 0.038822 | >=0.05 (OR amplitude condition) |
| Amplitude interpolated at crossing | 0.088546 | no smooth exception granted: K=1.32014 exceeds 0.1 |
| Crossing-tail gate | K=1.32014; tail/top-two energy=0.00394232 (0.3942%) | K>0.4 AND energy ratio>0.035 (3.5%) |

Here normalized radial energy means `sum_h xi_h(r)^2` divided by its maximum
over radius, not an integrated fraction of total energy. There are three
true crossings, all upper-boundary, at r=0.431548, 0.444124 and 0.502668;
the first two carry negligible amplitude. No crossing window violates its
original thresholds, so the smooth-crossing exception does not cause the pass.

The strongest lobe's width is a full signed-amplitude half-maximum width,
not a half-width or an energy width. Its width is 16.7% above the current
one-interval limit. The narrow-envelope gate has sufficient width sensitivity
but its radial cutoff excludes the r=0.555 peak.

A diagnostic-only call to the shared continuum-extremum extractor with
`r_max=0.7` finds the nearby upper minimum at r=0.55: `ext_dr=0.005`,
`ext_df_gap=0.000807448526` (0.0807%), and `ext_energy_frac=0.296112`.
That clearance is below the strict 0.1% minimum required by the
interior-envelope extremum exception. The production extractor's search and
gate are confined to r<=0.5, so this minimum does not enter that gate's
decision. These wider-search values are supplemental, not the saved active
feature values. Extending radial coverage would require a separate audit.

Overall rule severity is 0.856842, with `BAD_GRID_SCALE_SPIKE` the nearest
gate; crossing-window severity is 0.819773. Raw signed-profile inspection
confirms the narrow peak and crossing feature. This identifies a limitation
of the chosen cuts and radial coverage, rather than a stale export.

Reproduce from the configured NOVA environment:

```tcsh
python scripts/sort_shot_rules.py --rule_config tae_rules_production_v11 --shot_dir "$NOVA_DITW_ROOT/nstxuE204678M01t017" --n_min 9 --n_max 9 --pattern egn09w.1081E+02 --out_dir outputs/review_n9_1081_20260913/rules
```

The local raw-profile figure is
`outputs/review_n9_1081_20260913/profile_and_crossing.png`; complete calibration
exports are alongside it in `rules/`. Generated artifacts remain ignored.
This diagnostic follow-up changes no gate, label or production output.
